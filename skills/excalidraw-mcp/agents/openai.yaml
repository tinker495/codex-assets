interface:
  display_name: "Excalidraw MCP"
  short_description: "Plan, draw, and revise diagrams through Excalidraw MCP"
  default_prompt: "Use $excalidraw-mcp to plan, draw, and iteratively revise this diagram through Excalidraw MCP with camera moves and checkpoints."

dependencies:
  tools:
    - type: "mcp"
      value: "excalidraw"
      description: "Excalidraw MCP server"

interface:
  display_name: "Branch Health Remediation Workflow"
  short_description: "Onboard branch, run health plus targeted localization, propose plan."
  default_prompt: "Run onboarding, code health, targeted non-test localization, then propose remediation plan."

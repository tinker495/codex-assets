interface:
  display_name: "Main Merge"
  short_description: "Branch onboarding after main merge, conflict resolution, and document refresh"
  default_prompt: "Use $main-merge to onboard the current branch, merge main, resolve conflicts, and refresh branch documentation."

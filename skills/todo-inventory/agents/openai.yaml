interface:
  display_name: "TODO Inventory"
  short_description: "List TODOs with git-aware current and diff scans"
  default_prompt: "Use $todo-inventory to list remaining TODOs in this repo with the default git-aware current scan and summarize any TODOs newly added in the current diff."
